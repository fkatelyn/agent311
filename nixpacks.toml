[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["cd agent311 && uv sync --frozen --no-cache"]

[start]
cmd = "cd agent311 && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
