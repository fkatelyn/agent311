providers = ["node"]
# Node.js provider for Next.js assistant-ui frontend

[variables]
NEXT_PUBLIC_API_URL = "https://agent311-production.up.railway.app"

[build]
cmd = "npm install && npm run build"

[start]
cmd = "npm start"
